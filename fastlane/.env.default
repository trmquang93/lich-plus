# ===========================================
# App Store Connect API Key
# ===========================================
# Get these from App Store Connect > Users and Access > Keys
ASC_KEY_ID=YOUR_KEY_ID_HERE
ASC_ISSUER_ID=YOUR_ISSUER_ID_HERE
ASC_KEY_CONTENT=BASE64_ENCODED_P8_KEY_CONTENT

# ===========================================
# Fastlane Match (Code Signing)
# ===========================================
# Local path or Git URL for certificates repository
MATCH_GIT_URL=/Users/quang.tranminh/Projects/new-ios/ios-certificates

# Password to encrypt certificates in the repository
# Generate a secure password: openssl rand -base64 32
MATCH_PASSWORD=YOUR_SECURE_MATCH_PASSWORD

# ===========================================
# Certificate Password (for .p12 files)
# ===========================================
CERT_PASSWORD=YOUR_P12_PASSWORD

# ===========================================
# CI/CD (GitHub Actions)
# ===========================================
# For HTTPS auth: base64(username:personal_access_token)
# MATCH_GIT_BASIC_AUTH=YOUR_BASE64_AUTH_TOKEN

# Keychain settings for CI
# MATCH_KEYCHAIN_NAME=fastlane_keychain
# MATCH_KEYCHAIN_PASSWORD=temporary_keychain_password
